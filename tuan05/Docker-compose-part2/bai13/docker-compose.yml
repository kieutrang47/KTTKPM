version: "3.8"

services:
  web_react:
    image: nginx:alpine
    container_name: nginx_react_bai13
    ports:
      - "3001:80"
    volumes:
      - ./html:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
